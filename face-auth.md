# 顔認証システム（face-auth.html）

ブラウザだけで動く 1:N の顔認証システムです。顔の検出・特徴量抽出・照合をすべて端末内（TensorFlow.js）で行い、
顔画像や特徴量を外部に送信しません。

## 起動

カメラ（getUserMedia）とモデルファイルの読み込みには **http(s) 経由のアクセス** が必要です。
`file://` で直接開くとカメラを開始できません。

```bash
cd <このリポジトリ>
python3 -m http.server 8000
# ブラウザで http://localhost:8000/face-auth.html を開く
```

別端末から使う場合は https で配信してください（localhost 以外はカメラが許可されません）。

## iPad / スマホで試す

iOS Safari はカメラの利用に **https（または localhost）** を要求します。LAN の `http://192.168.x.x:8000` では
カメラが起動しないため、次のどちらかを使ってください。

### A. GitHub Pages で配信する（PC不要）

1. GitHub → リポジトリの **Settings → Pages** を開く
2. Source を「Deploy from a branch」、Branch にこのブランチ（または `main`）と `/ (root)` を指定して Save
3. 1〜2分待って、iPad の Safari で `https://<ユーザー名>.github.io/a/face-auth.html` を開く

ページを公開しても、**登録した顔のデータは開いた端末のブラウザ内（localStorage）にしか保存されません**。
サーバーにも他の閲覧者にも渡りません。

### B. PC でサーバーを立ててトンネル経由で開く

PC 側で `python3 -m http.server 8000` を動かし、`cloudflared tunnel --url http://localhost:8000` などで
https の一時URLを発行して、そのURLを iPad で開きます。

### iPad で使うときのポイント

- Safari が出すカメラ許可のダイアログで「許可」を選ぶ（拒否した場合は AA ボタン →「Webサイトの設定」から変更）
- 前面カメラは画角が広く顔が小さく写りがちです。検出解像度は「自動」のままにしてください
- 初回は顔検出モデルの初期化に数秒かかります。「顔を検出中…」のまま数秒待ってください

## 使い方

1. **顔を登録**タブ … 名前を入れ、本人同意にチェック → カメラを開始 →
   画面の案内（正面 / 右 / 左 / 下 / 上）に従って5枚撮影 → 「この顔を登録する」。
   角度違いを複数枚登録するほど認証が安定します。カメラが無い場合は「写真から登録」で1枚から登録できます。
2. **認証**タブ … カメラに顔を向けると自動で照合します。同一人物として連続一致した回数がしきい値に達すると確定します。
   「写真から照合」で画像ファイルを照合することもできます。
3. **管理・設定**タブ … 登録者の一覧・名前変更・削除、JSON の書き出し／読み込み、認証パラメータの調整。

## 仕組み

| 段階 | 使用モデル | 内容 |
| --- | --- | --- |
| 顔検出 | YuNet（227KB・MIT） | stride 8/16/32 を同時に見るマルチスケール検出器。顔の大きさによらず1パスで検出 |
| ランドマーク | MediaPipe Face Landmarker | 検出した顔の周辺を切り出し、478点のランドマークと表情スコアを取得 |
| 位置合わせ | （計算のみ） | 両目と口の重心から顔を切り出す（dlib方式） |
| 特徴量抽出 | 顔認識モデル（ResNet系・TensorFlow.js） | 顔を 128 次元ベクトルに変換 |
| 照合 | ユークリッド距離 | 登録済みベクトルとの距離が小さいほど同一人物 |

登録者は各サンプルごとに特徴量を保持し、照合時は**全サンプル中の最小距離**を採用します。

### なぜ MediaPipe に替えたか

もともとは TinyFaceDetector（顔検出）+ 68点ランドマークを使っていましたが、
**入力解像度によって検出できる顔の大きさが変わる**という性質がありました（実測、フレーム幅に対する顔の比率ごとの検出スコア）。

| 顔の大きさ | 160px | 224px | 320px | 416px | MediaPipe |
| --- | --- | --- | --- | --- | --- |
| 70% | 77 | 87 | 54 | 0 | 検出 |
| 45% | 71 | 86 | 0 | 0 | 検出 |
| 30% | 71 | 63 | 69 | 77 | 検出 |
| 20% | 0 | 57 | 58 | 73 | 検出 |
| 12% | 0 | 0 | 63 | 69 | — |

解像度を巡回させて回避していましたが、**巡回のたびに切り出し位置が変わり、特徴量が揺れる**という副作用がありました。
同じ写真でも解像度が違うだけで距離が 0.10〜0.28 動きます。

MediaPipe に替えると切り出しが安定し、実測で次のようになりました。

| | 従来 | MediaPipe経由 |
| --- | --- | --- |
| 同一人物（加工した画像） | 0.191 | **0.084** |
| 別人 | 0.620 | **0.757** |
| 同じ画像を2回 | 0.000 | 0.000 |

本人と他人の差が約3倍から約9倍に広がっています。

### 検出は YuNet に置き換えました

MediaPipe の顔検出（BlazeFace）は近〜中距離向けの固定レンジ検出器で、小さな顔を落とします。
そこで検出だけを **YuNet**（OpenCV Zoo / Shiqi Yu 氏・MIT・227KB）に置き換えました。
YuNet は stride 8/16/32 の3階層を同時に見るマルチスケール検出器なので、1パスで全ての大きさの顔を拾えます。

検出した顔の周辺を切り出してから MediaPipe にかけるため、MediaPipe は常に「顔が大きく写った画像」を見ることになり、
その得意領域から外れません。

実測（フレーム幅に対する顔の比率ごと）：

| 顔の大きさ | 旧（解像度巡回の最良スコア） | MediaPipe単独 | YuNet→切出→MediaPipe |
| --- | --- | --- | --- |
| 70% | 87 | 検出 | 検出(89) |
| 60% | 78 | 検出 | 検出(91) |
| 45% | 86 | 検出 | 検出(91) |
| 30% | 77 | 検出 | 検出(90) |
| 20% | 73 | 検出 | 検出(90) |
| 12% | 69 | — | 検出(87) |
| 8% | — | — | 検出(69) |

設定の「検出スコアの下限」は YuNet のしきい値です（低いほど遠くの顔まで拾います）。
「検出解像度」は、YuNet も MediaPipe も使えない環境の予備経路でのみ使われます。

### 認識モデルの切り替え（JAPANESE FACE V1）

特徴量を作るモデルは2つから選べます（管理・設定 → 認識モデル）。

| | 標準（同梱） | JAPANESE FACE V1 |
| --- | --- | --- |
| 中身 | dlib系 ResNet | EfficientNetV2 + ArcFace |
| 次元 | 128 | 512 |
| 比較方法 | ユークリッド距離（**小さい**ほど似ている） | コサイン類似度（**大きい**ほど似ている） |
| 既定しきい値 | 0.45 | 0.40 |
| 追加ダウンロード | なし | 約40MB（初回のみ） |
| ライセンス | 商用可 | 研究・非稼働はApache-2.0、**商用の実稼働は要契約** |

JAPANESE FACE V1 は yKesamaru 氏（東海顔認証）が公開している、日本人の顔に最適化された学習モデルです。
作者の計測では、若年日本人女性の顔画像に対して dlib の AUC 0.94 に対し 0.98 を達成したとされています。

同梱の検証用画像（日本人ではありません）での実測値：

| | 標準 | JAPANESE FACE V1 |
| --- | --- | --- |
| 同一人物（加工した画像） | 距離 0.084 | 類似度 **0.961** |
| 別人 | 距離 0.757 | 類似度 **0.096** |

しきい値0.4に対して、本人0.96・他人0.10と非常に大きく離れています。

**ライセンスの注意**：研究目的、および「まだ稼働させていない」商用利用（社内実験・試験運用など）は
Apache License 2.0 の条件で使用できます。商用サービスとして**実稼働**させる場合は、
作者との別途ライセンス契約が必要です（連絡先は `vendor/japanese-face/LICENSE.md` に記載）。
そのため既定は標準モデルのままにしてあります。

**切り替えると登録し直しが必要です**：特徴量の次元も比較方法も変わるため、
別のモデルで登録した人とは照合できません。一覧には「別の認識モデル」と表示されます。

処理系は ONNX Runtime Web（MIT）で、モデルの入力は dlib の `get_face_chip`（224px・padding 0.1）相当の
アフィン切り出し、ImageNet正規化です。ブラウザでの出力が Python の onnxruntime と一致することを
確認しています（コサイン類似度 0.9996）。

### 登録データの互換性

MediaPipe 経由の特徴量は、従来経路のものと平均 0.16 ほどずれます（しきい値0.45に対しては通る範囲で、
もともと解像度の巡回で 0.10〜0.28 揺れていたことを考えれば同程度）。
切り替え前に登録した人もそのまま照合できますが、精度を出したい場合は登録し直すか、サンプルを追加してください。

## 設定の目安

| 項目 | 既定 | 説明 |
| --- | --- | --- |
| 一致しきい値 | 0.45 | 小さいほど厳格。0.4〜0.5 が実用域。他人受入が多ければ下げ、本人拒否が多ければ上げる |
| 連続一致の回数 | 3 | 同一人物が連続一致したら確定。増やすほど誤認証が減り、確定は遅くなる |
| 生体検知（まばたき） | OFF | ON にすると、直近10秒以内にまばたきを検知しないと確定しない |
| 検出解像度 | 自動 | 固定すると、その解像度で写らない大きさの顔は検出できなくなる |
| 検出スコアの下限 | 0.50 | 低いほど顔以外を拾いやすくなる |

参考値（同梱モデル・実測）：同一人物の別条件画像で 0.19、別人同士で 0.51〜0.66。

## データの保存先と削除

- 保存先はブラウザの `localStorage`（キー：`faceauth.people.v1` / `faceauth.log.v1` / `faceauth.config.v1`）。サーバー送信はありません。
- 端末・ブラウザごとに独立しています。移行は「JSONで書き出し／読み込み」で行います。
- 書き出した JSON には**顔の特徴量とサムネイル画像**が含まれるため、個人情報として管理してください。
- 「全データを削除」で登録者・ログ・設定をまとめて削除できます。個別削除は登録者一覧の「削除」から。

## 制限と運用上の注意

- 照明・角度・マスク・経年で精度は下がります。双子や近親者は誤認する場合があります。
- まばたき検知は簡易的なもので、動画を再生した提示攻撃などは防げません。**入退室管理や決済など重要な用途では、
  暗証番号やカードと併用する多要素構成**を前提にしてください。
- 顔情報は個人情報（個人情報保護法の「個人識別符号」）に当たります。取得目的の明示・本人同意・保管期間・削除手順を定めて運用してください。
- 本人拒否／他人受入のバランスはしきい値で調整します。導入前に実利用環境（照明・カメラ位置）で必ず検証してください。

## 同梱物

```
face-auth.html            アプリ本体
face-engine.js            顔検出・照合の共通処理（kintai.html と共有）
vendor/face-api/          @vladmandic/face-api 1.7.15（MIT）
  face-api.js             ライブラリ本体（TensorFlow.js 同梱）
  model/                  学習済み重み（顔認識 / 予備経路の検出・ランドマーク）
vendor/mediapipe/         @mediapipe/tasks-vision 1.0.1（Apache-2.0）
  vision_bundle.mjs       ライブラリ本体
  wasm/                   WebAssembly ランタイム（約12MB）
  face_landmarker.task    学習済みモデル（約3.7MB）
vendor/yunet/             YuNet 顔検出モデル（MIT・227KB）
vendor/onnxruntime/       ONNX Runtime Web 1.27.0（MIT）※YuNet と JAPANESE FACE V1 が使用
vendor/japanese-face/     JAPANESE_FACE_V1.onnx（約26MB）+ ライセンス
```

初回の読み込みは合計20MBほどです（2回目以降はキャッシュされます）。
JAPANESE FACE V1 を選ぶと、そのときに追加で約40MBを取得します。

モデルはローカル同梱を優先し、読み込みに失敗した場合のみ jsDelivr の CDN にフォールバックします（オフラインでも動作します）。

## キオスク（index.html）への組み込み

認証結果を他ページから使う場合は、登録データを読んで同じ距離計算をするだけです。

```js
const people = JSON.parse(localStorage.getItem('faceauth.people.v1') || '[]');
// descriptor（128次元）同士のユークリッド距離が 0.45 以下なら同一人物とみなす
```

「いつもの注文」を出すなら、`people[].note` に商品名を入れておくと、認証成功時にそのまま参照できます。
