# 勤怠システム（kintai.html）

共用のiPad 1台に全員が顔を向けて打刻する、出勤・退勤の記録システムです。
顔の照合はすべて端末内で行い、打刻記録もこの端末のブラウザに保存します。サーバーは不要です。

## なぜ顔認証なのか（パスキーではなく）

共用端末ではパスキー（Face ID）は本人確認になりません。iPadのFace IDは端末の持ち主1人分しか登録できないため、
店に置いた1台に全員のパスキーを入れても、解錠できるのは登録された1人だけになり、その人が全員分を打刻できてしまいます。
「いま誰が立っているか」を見分けられるのは顔認証のほうなので、打刻の本人確認には顔認証を使っています。

パスキーは**管理画面のロック**に使っています。こちらは「この端末の管理者＝持ち主だけが開ける」という用途なので、
端末に紐づくパスキーが適しています。

## 起動

```bash
python3 -m http.server 8000
# → http://localhost:8000/kintai.html
```

GitHub Pages に置いてある場合は `https://<ユーザー名>.github.io/a/kintai.html` を iPad の Safari で開きます。
カメラは https か localhost でないと使えません。パスキーも同様で、IPアドレス（`http://192.168.x.x`）では登録できません。

## 使い方

### 1. 従業員を登録する（管理 → 従業員）

氏名を入れ、本人同意にチェックを入れて、案内どおり5枚撮影します。角度違いを複数登録するほど認識が安定します。
`face-auth.html` で登録済みの人がいれば「顔認証システムの登録者を取り込む」で一括で移せます。

### 2. 打刻する（打刻タブ）

カメラに顔を向けると自動で照合され、名前と「出勤」「退勤」ボタンが出ます。次に押すべきほうに「（推奨）」が付きます。
押すと打刻され、4秒後に次の人の待機画面に戻ります。

- 直近の同じ種別の打刻から5分以内（設定で変更可）は、二重打刻の確認が出ます
- 登録されていない顔では打刻できません

### 3. 確認する（本日タブ）

いま勤務中の人、本日の打刻一覧、本日の実働時間が見られます。

### 4. 締める（管理 → 月次集計・CSV）

月を選んで日別の出勤・退勤・実働と、月合計（出勤日数・実働合計）を確認し、CSVを書き出します。

- **日別CSV**：日付・曜日・氏名・出勤・退勤・実働（時:分と分の両方）・備考
- **打刻明細CSV**：打刻1件ごとの日時・氏名・種別・方法（顔認証/手修正）・顔の距離

CSVは Excel で文字化けしないよう UTF-8 BOM 付きで書き出します。

## 打刻の修正

管理 → 打刻の修正から、打刻の追加・時刻変更・削除ができます。手で触った打刻は「手修正」として記録に残るので、
あとから顔認証による打刻と区別できます。

## 勤務時間の計算

出勤→退勤を時系列でペアにし、日付は「出勤打刻の日」に属するものとして扱います（日をまたぐ勤務にも対応）。

- 退勤忘れ → その日は「退勤打刻なし」と表示され、実働は空欄になります
- 出勤忘れ → 「出勤打刻なし」として退勤だけの行が出ます
- 勤務中 → 実働欄は「勤務中」

実働は分単位でそのまま集計します。労働時間は1分単位での把握が原則で、日ごとの切り捨ては認められていません
（月の合計で30分未満を四捨五入する扱いのみ例外的に許容されています）。そのため日次の丸め機能はあえて入れていません。

## 管理画面のロック（パスキー）

管理 → 設定 →「このiPadのパスキーを設定」で、管理画面がFace ID / Touch ID / パスコードでロックされます。

**強度についての注意**：このロックはサーバーを持たないため、署名の検証をブラウザ内で行っています。
従業員がうっかり設定を触ったり記録を消したりするのを防ぐ程度の強度であり、
開発者ツールを使える人が迂回することは可能です。給与計算に使う確定データはCSVに書き出して別途保管してください。

## Googleスプレッドシート連携（複数端末から集計を見る）

打刻するたびに Google スプレッドシートへ1行追加し、「日別」シート（日付・氏名・出勤・退勤・実働）を自動で作り直します。
シートを共有すれば、店長のスマホやPCからそのまま勤怠表を見られます。**打刻するのは引き続きiPad 1台**です。

### セットアップ（10分ほど）

1. **スプレッドシートを作る**
   [sheets.new](https://sheets.new) で新しいスプレッドシートを作り、名前を「勤怠」などに変更します。
   （iPadの場合は Safari で開き、共有ボタン →「デスクトップ用Webサイトを表示」にすると次の手順のメニューが出ます）

2. **Apps Script を開く**
   メニューの「拡張機能」→「Apps Script」。

3. **コードを貼る**
   エディタの中身をすべて消し、このリポジトリの `gas/kintai-sheet.gs` の中身を貼り付けます。
   先頭の

   ```js
   var TOKEN = "kintai-himitsu-2026";
   ```

   を、**自分で決めた合言葉**に書き換えて保存します（あとでiPad側にも同じ文字列を入れます）。

4. **デプロイする**
   右上の「デプロイ」→「新しいデプロイ」→ 種類の選択（歯車）で「**ウェブアプリ**」を選び、

   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**

   でデプロイ。初回は Google の許可画面が出るので、「詳細」→「（プロジェクト名）に移動」→ 許可、と進みます。
   最後に表示される `https://script.google.com/macros/s/…/exec` というURLをコピーします。

   > 「アクセスできるユーザー：全員」は、URLを知っている人なら誰でもアクセスできる状態です。
   > そのため合言葉（TOKEN）による確認を入れてあります。URLと合言葉は他人に教えないでください。

5. **iPad側に登録する**
   勤怠システムの 管理 → 設定 →「Googleスプレッドシート連携」に、URLと合言葉、この端末の名前（例：レジ横iPad）を入力し、
   「接続テスト」を押します。「接続できました」と出れば完了です。

### 使い方

- 打刻すると自動で送信されます（打刻画面の下に「スプレッドシートと同期済み」と出ます）
- 電波が悪いなどで送れなかったときは端末に貯まり、「未送信の打刻が N 件あります」と表示され、次の打刻時に自動で再送します
- 手動で送りたいときは 管理 → 設定 →「今すぐ全件送信」。打刻IDで重複を除くので、何度押しても行は増えません
- 集計を見せたい相手には、**スプレッドシートを共有**してください（Apps ScriptのURLではなく、シートそのものを共有します）

### 注意

- 端末側にも記録は残り続けます。スプレッドシートは「集計を見るための写し」と考えてください
- 打刻の**時刻を修正**すると、次の送信でシート側の同じ行が上書きされます（行は増えません）
- 打刻の**削除**はシートには伝わりません。シート側は記録が残る台帳として扱い、必要なら手で消してください
- 送信できるのは打刻だけです。顔の特徴量は端末から一切出ません

## データについて

| 保存先 | localStorage（このブラウザの中だけ） |
| --- | --- |
| `kintai.employees.v1` | 従業員（氏名・顔の特徴量・サムネイル） |
| `kintai.punches.v1` | 打刻記録 |
| `kintai.config.v1` | しきい値などの設定 |
| `kintai.admin.v1` | 管理画面のパスキー（公開鍵と資格情報ID） |

**Safari の履歴・Webサイトデータを消すと打刻記録も消えます。**
月末のCSV書き出し、または管理 → 設定 →「全データをJSONで書き出し」による定期バックアップを運用に組み込んでください。

## 運用上の注意

- **なりすまし**：写真をかざす不正は完全には防げません。打刻の不正が問題になる環境では、記録の確認・監査を併用してください
- **個人情報**：顔の特徴量は個人情報保護法の「個人識別符号」にあたります。取得目的の明示と本人同意、
  退職時の削除（従業員一覧の「削除」）を運用に組み込んでください
- **認識されにくいとき**：眼鏡の有無や照明で距離が伸びることがあります。管理 → 従業員 →「今の顔をサンプル追加」で、
  その状態のまま追加登録すると安定します。それでも通らない場合は設定でしきい値を上げてください（既定 0.45）
- **「旧方式で登録」と出ている人**：顔の切り出し方法を変更する前に登録された人です。そのままでも照合できますが、
  「今の顔をサンプル追加」で1枚足すと安定します

## 構成

```
kintai.html          勤怠システム本体
gas/kintai-sheet.gs  Googleスプレッドシート連携用のApps Scriptコード
face-auth.html       顔認証の単体デモ（しきい値の挙動を確かめる用）
face-engine.js       顔検出・照合の共通処理（両方から読み込む）
vendor/mediapipe/    @mediapipe/tasks-vision 1.0.1（Apache-2.0）と顔ランドマークモデル
vendor/face-api/     @vladmandic/face-api 1.7.15（MIT）と顔認識モデル
```

顔の検出とランドマークは MediaPipe、特徴量の抽出は face-api の認識モデルという構成です。
詳しい経緯と実測値は `face-auth.md` の「仕組み」を参照してください。
初回の読み込みは合計20MBほどになります（2回目以降はキャッシュされます）。
